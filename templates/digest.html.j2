<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deal Digest - {{ date }}</title>
</head>
<body style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px; background-color: #f5f5f5;">
    <!-- Header -->
    <div style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 24px; border-radius: 12px 12px 0 0; text-align: center;">
        <h1 style="margin: 0 0 8px 0; font-size: 28px;">Deal Digest</h1>
        <p style="margin: 0; opacity: 0.9; font-size: 16px;">{{ date }}</p>
        <p style="margin: 12px 0 0 0; font-size: 14px; opacity: 0.8;">
            {{ promo_count }} deal{% if promo_count != 1 %}s{% endif %} from {{ store_count }} store{% if store_count != 1 %}s{% endif %}
        </p>
    </div>

    <!-- Main Content -->
    <div style="background: white; padding: 24px; border-radius: 0 0 12px 12px; box-shadow: 0 2px 8px rgba(0,0,0,0.1);">
        {% if report %}
        <!-- Run Performance -->
        <div style="margin-bottom: 28px; background: #f8fafc; border-radius: 10px; padding: 16px; border: 1px solid #e2e8f0;">
            <h2 style="margin: 0 0 12px 0; font-size: 18px; color: #0f172a;">Run Performance</h2>
            <p style="margin: 0 0 8px 0; color: #334155; font-size: 13px;">
                Generated at: {{ report.generated_at }}
            </p>
            <div style="font-size: 13px; color: #334155; line-height: 1.6;">
                <div><strong>Ingest:</strong>
                    Gmail fetched {{ report.ingest.gmail.fetched if report.ingest and report.ingest.gmail else 0 }},
                    Web signals {{ report.ingest.web.signals if report.ingest and report.ingest.web else 0 }},
                    New {{ report.ingest.web.new if report.ingest and report.ingest.web else 0 }},
                    Errors {{ report.ingest.web.errors if report.ingest and report.ingest.web else 0 }}
                </div>
                <div><strong>Extract:</strong>
                    Processed {{ report.extract.processed if report.extract else 0 }},
                    Succeeded {{ report.extract.succeeded if report.extract else 0 }},
                    Failed {{ report.extract.failed if report.extract else 0 }}
                </div>
                <div><strong>Merge:</strong>
                    Created {{ report.merge.created if report.merge else 0 }},
                    Updated {{ report.merge.updated if report.merge else 0 }}
                </div>
            </div>

            {% if report.web_failures %}
            <h3 style="margin: 14px 0 8px 0; font-size: 15px; color: #7f1d1d;">Errors / Non-success Attempts</h3>
            <table style="width: 100%; border-collapse: collapse; font-size: 12px;">
                <thead>
                    <tr>
                        <th style="text-align: left; padding: 6px; border-bottom: 1px solid #e2e8f0;">Store</th>
                        <th style="text-align: left; padding: 6px; border-bottom: 1px solid #e2e8f0;">Method</th>
                        <th style="text-align: left; padding: 6px; border-bottom: 1px solid #e2e8f0;">Status</th>
                        <th style="text-align: left; padding: 6px; border-bottom: 1px solid #e2e8f0;">Reason</th>
                    </tr>
                </thead>
                <tbody>
                    {% for attempt in report.web_failures %}
                    <tr>
                        <td style="padding: 6px; border-bottom: 1px solid #f1f5f9;">{{ attempt.store }}</td>
                        <td style="padding: 6px; border-bottom: 1px solid #f1f5f9;">{{ attempt.source_type }}</td>
                        <td style="padding: 6px; border-bottom: 1px solid #f1f5f9;">{{ attempt.status }}</td>
                        <td style="padding: 6px; border-bottom: 1px solid #f1f5f9;">{{ attempt.message or attempt.error_code }}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            {% endif %}

            {% if report.web_attempts %}
            <h3 style="margin: 14px 0 8px 0; font-size: 15px; color: #0f172a;">All Source Attempts</h3>
            <table style="width: 100%; border-collapse: collapse; font-size: 12px;">
                <thead>
                    <tr>
                        <th style="text-align: left; padding: 6px; border-bottom: 1px solid #e2e8f0;">Store</th>
                        <th style="text-align: left; padding: 6px; border-bottom: 1px solid #e2e8f0;">Method</th>
                        <th style="text-align: left; padding: 6px; border-bottom: 1px solid #e2e8f0;">Status</th>
                        <th style="text-align: left; padding: 6px; border-bottom: 1px solid #e2e8f0;">Signals</th>
                        <th style="text-align: left; padding: 6px; border-bottom: 1px solid #e2e8f0;">Requests</th>
                    </tr>
                </thead>
                <tbody>
                    {% for attempt in report.web_attempts %}
                    <tr>
                        <td style="padding: 6px; border-bottom: 1px solid #f1f5f9;">{{ attempt.store }}</td>
                        <td style="padding: 6px; border-bottom: 1px solid #f1f5f9;">{{ attempt.source_type }}</td>
                        <td style="padding: 6px; border-bottom: 1px solid #f1f5f9;">{{ attempt.status }}</td>
                        <td style="padding: 6px; border-bottom: 1px solid #f1f5f9;">{{ attempt.signals }}</td>
                        <td style="padding: 6px; border-bottom: 1px solid #f1f5f9;">{{ attempt.http_requests }}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            {% endif %}
        </div>
        {% endif %}
        {% for store_name, promos in stores.items() %}
        <!-- Store Section -->
        <div style="margin-bottom: 32px;">
            <h2 style="color: #333; font-size: 20px; margin: 0 0 16px 0; padding-bottom: 8px; border-bottom: 2px solid #eee;">
                {{ store_name }}
            </h2>

            {% for item in promos %}
            <!-- Promo Card -->
            <div style="background: #fafafa; border-radius: 8px; padding: 16px; margin-bottom: 12px; border-left: 4px solid {% if item.badge == 'NEW' %}#22c55e{% else %}#3b82f6{% endif %};">
                <!-- Badge -->
                <span style="display: inline-block; padding: 4px 8px; border-radius: 4px; font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 8px; {% if item.badge == 'NEW' %}background: #dcfce7; color: #166534;{% else %}background: #dbeafe; color: #1e40af;{% endif %}">
                    {{ item.badge }}
                </span>

                <!-- Headline -->
                <h3 style="color: #111; font-size: 16px; margin: 8px 0; line-height: 1.4;">
                    {{ item.promo.headline }}
                </h3>

                <!-- Discount -->
                {% if item.promo.discount_text %}
                <p style="color: #dc2626; font-size: 18px; font-weight: 600; margin: 8px 0;">
                    {{ item.promo.discount_text }}
                </p>
                {% endif %}

                <p style="color: #64748b; font-size: 12px; margin: 6px 0;">
                    Found via: <strong>{{ item.source_type }}</strong>
                    {% if item.source_url %}
                    &nbsp;·&nbsp; Source URL:
                    <a href="{{ item.source_url }}" style="color: #2563eb; text-decoration: none;">{{ item.source_url }}</a>
                    {% endif %}
                </p>

                <!-- Code -->
                {% if item.promo.code %}
                <div style="margin: 12px 0;">
                    <span style="font-size: 12px; color: #666; text-transform: uppercase;">Code:</span>
                    <code style="display: inline-block; background: #fef3c7; color: #92400e; padding: 6px 12px; border-radius: 4px; font-family: 'SF Mono', 'Consolas', monospace; font-size: 14px; font-weight: 600; letter-spacing: 1px; margin-left: 8px;">
                        {{ item.promo.code }}
                    </code>
                </div>
                {% endif %}

                <!-- End Date -->
                {% if item.promo.ends_at %}
                <p style="color: #666; font-size: 13px; margin: 8px 0;">
                    <span style="color: #ef4444;">⏰</span>
                    Ends: {{ item.promo.ends_at.strftime('%B %d, %Y') }}
                    {% if item.promo.end_inferred %}<span style="color: #9ca3af;">(estimated)</span>{% endif %}
                </p>
                {% endif %}

                <!-- Shop Link -->
                {% if item.promo.landing_url %}
                <a href="{{ item.promo.landing_url }}" style="display: inline-block; margin-top: 12px; padding: 10px 20px; background: #111; color: white; text-decoration: none; border-radius: 6px; font-size: 14px; font-weight: 500;">
                    Shop Now →
                </a>
                {% endif %}
            </div>
            {% endfor %}
        </div>
        {% endfor %}

        {% if not stores %}
        <p style="text-align: center; color: #666; padding: 40px 0;">
            No new deals today. Check back tomorrow!
        </p>
        {% endif %}
    </div>

    <!-- Footer -->
    <div style="text-align: center; padding: 20px; color: #999; font-size: 12px;">
        <p style="margin: 0;">
            Generated by Deal Intelligence
        </p>
    </div>
</body>
</html>
